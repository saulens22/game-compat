# Steam games

Store each Steam case in `steam/<game-slug>-<app-id>/`. The public Steam App ID
identifies the exact store edition without exposing a user's account ID.

Each case separates user setup (`README.md`), sanitized final findings
(`RESULTS.md`), and optional agent procedure (`AGENTS.md`). Generated logs,
prefixes, evidence, downloads, and configuration snapshots remain ignored.
