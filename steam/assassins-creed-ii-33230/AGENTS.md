# Assassin's Creed II App ID 33230 research notes

- Verify the live `AssassinsCreedIIGame.exe`; Ubisoft Connect remaining open is
  not proof that the game started.
- Preserve the default Steam Input layout and fingerprint it around setup.
- Keep `ImproveDrawDistance=0` unless a changed EaglePatch version proves the
  Florence transition crash has been fixed.
- Do not publish Nexus-packed `.forge` archives. Link the player to the mod page
  and require a backup of the original forge before any manual replacement.
- Keep Ubisoft account data, launcher logs, saves, and screenshots under ignored
  local paths.
